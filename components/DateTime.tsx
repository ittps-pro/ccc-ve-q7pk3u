import * as dayjs from 'dayjs';
import 'dayjs/locale/ru-ru'; // import locale
dayjs.locale('ru-RU');

function setDateTime(data) {}

let actions = {
  create: {
    title: 'Create',
    action: 'CreateAction([1, 2, 3])',
    icon: 'PlusOutlined',
  },
};
